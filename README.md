### camover-go

```bash
➜  camover git:(main) ✗ ./camover 
  -a string
    	Single address.
  -i string
    	Input file of file.
  -o string
    	Ouput result to file.
  -p int
    	Number of pages you want to get from ZoomEye (default 100)
  -shodan string
    	Shodan API key for exploiting devices over Inernet.
  -t	Use threads for fastes work.
  -zoomeye string
    	Zoome API key for exploiting devices over Internet.

```

---
that's all

